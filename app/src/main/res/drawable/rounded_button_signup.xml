<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Nền đen -->
    <solid android:color="#383434" />

    <!-- Bo góc (radius) -->
    <corners android:radius="25dp" />

    <!-- Viền trắng -->
    <stroke
        android:width="1dp"
        android:color="@android:color/white" />
</shape>
